{
  "name": "tictactoe-3d",
  "dependencies": {
    "three": "^0.151.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  },
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build ./index.html --out-dir dist --public-url ./ --no-minify",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
